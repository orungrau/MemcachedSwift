import XCTest
@testable import MemcachedSwift

final class MemcachedSwiftTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(MemcachedSwift.text, "Hello, World!")
    }


    static var allTests = [
        ("testExample", testExample),
    ]
}
